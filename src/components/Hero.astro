---
// src/components/Hero.astro
---

<section class="section text-center">
  <div class="section-header">
    <h1 class="section-title text-gradient">
      Elevate Your Brand with <br>
     Exceptional Design Solutions
    </h1>
    <p class="section-subtitle">
       We design innovative websites, applications, and brands that are beautifully crafted for all users. Emphasizing accessibility, inclusivity, and driven by cutting-edge AI technology.
    </p>
  </div>
  <div class="flex flex-col sm:flex-row gap-4 justify-center mt-8">
    <a href="/consultation" class="btn btn-primary btn-lg">
      Contact Us
    </a>
    <a
      href="/works"
      class="btn btn-outline btn-lg"
      onclick="handleWorksClick(event)">
      Our Portfolio
    </a>
  </div>

  <script is:inline>
    function handleWorksClick(event) {
      // If already on homepage, scroll to works section
      if (window.location.pathname === '/') {
        event.preventDefault();
        const worksSection = document.querySelector('#works');
        if (worksSection) {
          worksSection.scrollIntoView({ behavior: 'smooth', block: 'start' });
        }
      }
      // Otherwise, the regular href="/works" will navigate to works page
    }
  </script>
</section>
